# Pinned documentation build dependencies (deterministic MkDocs site builds)
# Update intentionally (bump together, verify locally via `mkdocs build --strict`).
# Chosen to satisfy existing constraints: mkdocs>=1.6, mkdocs-material>=9.5
mkdocs==1.6.1
mkdocs-material==9.6.18
