apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: betting-geofencing_regulated_markets
  annotations:
    rulehub.id: betting.geofencing_regulated_markets
    rulehub.title: <Policy Title>
    rulehub.links: |
      - <https://reference-1>
      - <https://reference-2>
spec:
  validationFailureAction: audit # or enforce
  background: true
  rules:
  - name: <rule-name>
    match:
      any:
      - resources:
          kinds:
          - "*"
    validate:
      message: "betting.geofencing_regulated_markets: example validation"
      pattern:
        metadata:
          labels:
            <required-label>: "?*"
