# Example secrets for running GitHub Actions locally with nektos/act.
# Copy this file to `.github/act.secrets` and replace placeholder values as needed.
# Format: KEY=VALUE (one per line)

# Required by many workflows (docs publish, link-check, scorecard, release-please, GHCR login).
# Use a GitHub Personal Access Token if you need to push, create PRs, or access GHCR.
# Otherwise, a dummy value can work for read-only paths or when steps are skipped.
# Recommended scopes if pushing/commenting: repo, workflow. For GHCR pushes: packages:write.
GITHUB_TOKEN=ghp_your_personal_access_token_or_dummy
